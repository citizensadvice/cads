{
  "name": "cadesignsystem",
  "description": "Citizens Advice Design System",
  "version": "0.0.1",
  "homepage": "https://citizensadvice.github.io/ca-designsystem/",
  "private": true,
  "workspaces": [
    "framework/*"
  ],
  "scripts": {
    "setup": "yarn && lerna bootstrap",
    "clean": "yarn run clean-framework && rm -rf ./website/_site/ && rm -rf ./website/.sass-cache && rm -rf ./node_modules/ ",
    "predeploy": "yarn run build-docs",
    "clean-modules": "rm -rf ./node_modules/",
    "reset-modules": "yarn clean-modules && yarn setup",
    "build-docs": "cd ./website/ && bundle install && bundle exec jekyll build",
    "start-docs": "cd ./website/ && bundle install && bundle exec jekyll serve",
    "deploy-docs": "gh-pages -d ./website/_site/",
    "start-sandbox": "cd ./sandbox/ && bundle install && bundle exec jekyll serve",
    "build-framework": "lerna run build",
    "clean-framework": "lerna run clean",
    "bump-framework": "lerna version --skip-git",
    "publish-framework": "lerna publish"
  },
  "devDependencies": {
    "@cads/system": "0.0.1",
    "gh-pages": "^2.0.1",
    "lerna": "^3.4.3",
    "node-sass": "^4.10.0"
  }
}
